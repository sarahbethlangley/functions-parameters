const quarterValue = (notScary) => {
    const quarter = notScary * .25

    return quarter 

}

// Invoke the function and store the return value
const returnedNumber = quarterValue(100)


// Log the return value
console.log(returnedNumber)
